## Explanation
**111. Minimum Depth of Binary Tree**

In this problem we are tasked to find the minimum depth of a binary tree. The minimal depth of a binary tree will be the depth of the highest leaf node. We can therefore use breadth first search to traverse the tree. This is a good idea, since the first leaf node encountered is guaranteed to have the minimum depth of the tree. 